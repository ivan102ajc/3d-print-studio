# 📋 Инструкция по заполнению {{TODO_ЗАПОЛНИТЬ}}

## Что нужно заполнить в knowledge_base.json

### 1. Контакты компании
```json
"company": {
  "phone": "+7 (XXX) XXX-XX-XX",  // ← Ваш телефон
  "email": "info@protolab3d.ru"     // ← Ваш email
}
```

### 2. Размеры для партнёрских технологий

#### SLA (стереолитография)
```json
"technologies[1]": {
  "max_size": {
    "x": 145,  // ← Максимальный размер по X (мм)
    "y": 145,  // ← Максимальный размер по Y (мм)
    "z": 180   // ← Максимальный размер по Z (мм)
  }
}
```

#### SLS (селективное лазерное спекание)
```json
"technologies[2]": {
  "max_size": {
    "x": 340,  // ← Максимальный размер по X (мм)
    "y": 340,  // ← Максимальный размер по Y (мм)
    "z": 600   // ← Максимальный размер по Z (мм)
  }
}
```

#### DMLS (прямое лазерное спекание металла)
```json
"technologies[3]": {
  "max_size": {
    "x": 250,  // ← Максимальный размер по X (мм)
    "y": 250,  // ← Максимальный размер по Y (мм)
    "z": 300   // ← Максимальный размер по Z (мм)
  }
}
```

### 3. Цены на услуги

#### 3D-сканирование
```json
"services[2]": {
  "price": "от 2000 ₽ за объект"  // ← Ваша цена
}
```

#### Реверс-инжиниринг
```json
"services[3]": {
  "price": "от 3000 ₽ за объект"  // ← Ваша цена
}
```

### 4. Международная доставка
```json
"delivery": {
  "shipping": {
    "international": "СНГ, Европа, Азия (через СДЭК)"  // ← Ваши возможности
  }
}
```

## Как заполнить

1. Откройте файл `/public/knowledge_base.json`
2. Найдите все `{{TODO_ЗАПОЛНИТЬ}}` (Ctrl+F)
3. Замените на реальные данные
4. Сохраните файл
5. Пересоберите проект: `npm run build`

## Примеры значений

### Телефоны:
- `"+7 (495) 123-45-67"`
- `"+7 (926) 987-65-43"`

### Email:
- `"info@protolab3d.ru"`
- `"order@protolab3d.ru"`

### Размеры (примеры):
- SLA: `{"x": 145, "y": 145, "z": 180}` (Formlabs Form 3)
- SLS: `{"x": 340, "y": 340, "z": 600}` (EOS P 396)
- DMLS: `{"x": 250, "y": 250, "z": 300}` (EOS M 290)

### Цены:
- Сканирование: `"от 2000 ₽"`, `"от 3000 ₽"`, `"5000 ₽"`
- Реверс-инжиниринг: `"от 3000 ₽"`, `"от 5000 ₽"`, `"по договорённости"`

### Международная доставка:
- `"СНГ, Европа, Азия (через СДЭК)"`
- `"Только СНГ"`
- `"Не осуществляем"`
- `"По всему миру (DHL, FedEx)"`

## Проверка

После заполнения проверьте:
1. ✅ Нет ли оставшихся `{{TODO_ЗАПОЛНИТЬ}}`
2. ✅ JSON валиден (используйте https://jsonlint.com/)
3. ✅ Все кавычки закрыты
4. ✅ Нет лишних запятых

## Тестирование

После заполнения протестируйте бота:
1. Откройте сайт
2. Спросите: "Какой максимальный размер печати?"
3. Проверьте, что ответ содержит реальные размеры
4. Спросите: "Сколько стоит сканирование?"
5. Проверьте, что ответ содержит вашу цену

## Если что-то не работает

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что JSON валиден
3. Пересоберите проект: `npm run build`
4. Очистите кэш браузера (Ctrl+Shift+R)

---

**Заполните эти поля, и бот будет давать точные ответы!** 🎯
